<!-- # ESTO ES UN PARCIAL

	# método de ayuda para la creación de un div. voy a entregarlo al post
-->
<!--
	# y voy a imprimir el título de la entrada
	# la h permite que si el titulo tiene cosas extrañas, éste escape de ellas y permita 
	# que se muestre el titulo en formato html en el navegador
	# luego imprimirá el título, al igual que el anterior; h para salir de html inseguro
	# y simple_format para que muestre un formato html simple
-->
<%= div_for @post do %> 
	<h2><%= h(@post.title) %></h2>
	<%= simple_format h(@post.body) %>
<% end %>


<!-- 
	# Ahora podemos ir al archivo show, quitar le cuerpo y llamar al parcial:
	<p>
	  <strong>Title:</strong>
	  <%= @post.title %>
	</p>

	<p>
	  <strong>Body:</strong>
	  <%= @post.body %>
	</p> 

	por: 

	# render @post
-->